<div class="top-row ps-3 navbar navbar-dark">
    <div class="container-fluid">
        @* <a class="navbar-brand" href="">voeairlines-blazor</a> *@
        <img src="../images/voe-airlines-logo.png" class="img-fluid" alt="VoeAirlines">
        <button title="Navigation menu" class="navbar-toggler" @onclick="ToggleNavMenu">
            <span class="navbar-toggler-icon"></span>
        </button>
    </div>
</div>

<div class="@NavMenuCssClass" @onclick="ToggleNavMenu">
    <nav class="flex-column">
        <div class="nav-item px-3">
            <NavLink class="nav-link" href="" Match="NavLinkMatch.All">
                <span class="oi oi-home" aria-hidden="true"></span> Home
            </NavLink>
        </div>
        <div class="nav-item px-3">
            <NavLink class="nav-link" href="aeronave">
                <span class="oi oi-home" aria-hidden="true"></span> Aeronave
            </NavLink>
        </div>
        <div class="nav-item px-3">
            <NavLink class="nav-link" href="listarlogins">
                <span class="oi oi-list-rich" aria-hidden="true"></span> Listar Usuários
            </NavLink>
        </div>
        <div class="nav-item px-3">
            <NavLink class="nav-link" href="listaraeronaves">
                <span class="oi oi-list-rich" aria-hidden="true"></span> Listar Aeronaves
            </NavLink>
        </div>
    </nav>
</div>

@code {
    private bool collapseNavMenu = true;

    private string? NavMenuCssClass => collapseNavMenu ? "collapse" : null;

    private void ToggleNavMenu()
    {
        collapseNavMenu = !collapseNavMenu;
    }
}
